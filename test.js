const SCENE_CLASSES = /(?:\s*class\s+(?<name>\w+)\(Scene\):\s*)/g;
const contents = `from manim import *from addon.ext import (RickaymScene, ProjectInit,                      section, per_bullet_point)ProjectInit(config)class Episode3(Scene):    passclass Episode2(Scene):    passclass Episode1(Scene):    def __init__(self, *args, **kwargs):        super().__init__(*args, **kwargs)        self.production = True    @section()    def what_are_compilers(self):        self.camera.background_color = "#222222"        q = (Text(r"What are compilers?", font_size=20, weight=BOLD)        …=NORMAL,                    text="<b>1.</b> Program to compile, (compilation of DVDs back in the days) an idea to\\n"                         "deduce a collection from a given pile and make it into a collection."                         "::bull<b>2.</b> And So on and So On..."))                    .move_to(q.get_right())                    .shift(DOWN*1.2)                    .shift(RIGHT*2.6)                    .set_color("#61C0CD"))        self.play(*per_bullet_point(Create, dtls), run_time=3)`;
const sceneRes = contents.match(SCENE_CLASSES);
console.log(sceneRes.map(r => r.groups, sceneRes));
console.log(sceneRes, sceneRes[0].groups);